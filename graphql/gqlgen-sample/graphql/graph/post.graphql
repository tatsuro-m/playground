type Post {
    id: ID!
    title: String!
    createdAt: Time!
    updatedAt: Time!
}

input NewPost {
    title:String!
}

input DeletePost {
    id: ID!
}

type Mutation {
    createPost(input: NewPost): Post!
    deletePost(input: DeletePost): ID!
}

scalar Time
